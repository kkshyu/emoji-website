# 網站安全修正部署驗證

驗證日期：2026-09-06（台灣時間）。正式站：<https://www.emoji.tw/>。

## 發布版本

- 修正提交：`09af34126275c1ae017f45201bfbfbbb7002bfb5`。
- Zeabur deployment：`6a9c6c757066abe5dab30187`，狀態 `RUNNING`。
- 建置完成時間：2026-09-05 19:27:44 UTC。
- 正式執行環境回讀：Node `v22.23.2`、`NODE_ENV=production`、執行 commit 與上述提交一致；只確認 APP_SECRET 已設定，不記錄內容。
- 發布基於遠端 `8fa5ee2`，保留已合併的社群場地申請。發布 33 檔與清單及 SHA-256 全部一致；隔離發布版本完整測試 253 通過、0 失敗、0 跳過。

原工作目錄另一項後台改版已提交為 `81c9586`，其父提交為本次修正。上述正式運行驗證僅證明 `09af341`，不能據此宣稱後台改版已上線。本次保留其他工作的檔案與提交。

## 正式站唯讀驗證

檢查起始時間：2026-09-05 19:32:23 UTC。共 48 次 HTTP 請求，0 項失敗：

- 三語各 11 個主要頁面及活動申請，共 36 頁，均為 200 且共用版型已組裝。
- 三語 `/menu` 均以 301 導向對應的 `/space#menu`。
- `/api/health` 回 200，`ok`、`db`、`dbConfigured` 均為 true。
- `/api/public` 回 200，`content` 不含 `ig_access_token`，亦未發現 token、secret、password 或 API key 類型的鍵名；只記錄布林結果。
- 未登入的 `/api/state`、`/api/admin/event-applications` 均回 401。
- 所有受測回應均具 nosniff、SAMEORIGIN、Referrer-Policy、基礎 CSP 與 HSTS，沒有 X-Powered-By；受測 API 均有 no-store。
- `marked.js`、`purify.min.js`、`space-dir.js`、`fellow/app.js`、`nav.js` 均回 200，SHA-256 與發布版完全一致。

Codex 右側瀏覽器亦確認：正式空間頁三樓按鈕可展開並更新為 `#f3`，內容正常顯示；`/fellow#project` 可直接顯示聚落計畫，無需等待點擊才初始化。

詳細證據：[正式站回應與資產雜湊](稽核紀錄/2026-09-06_部署正式站驗證.json)。

## 仍未完成的範圍

- 舊 IG 憑證仍須撤銷／輪替；停止公開回傳不等同憑證已失效。
- 本部署版本仍有 4 筆 moderate 傳遞依賴告警，high／critical 為 0。後續 MinIO 補修已完成本機測試並降為 0 漏洞，尚未部署；詳見[MinIO 修復紀錄](MinIO依賴修復_2026-09-06.md)。
- 本次正式驗證沒有收付款、退款、社群發文、上傳或 Google 帳號登入操作，不能宣稱外部交易流程已完成正式端到端測試。

母檔一致性檢查通過，0 筆殘留；沒有修改母檔管轄的營運事實。
