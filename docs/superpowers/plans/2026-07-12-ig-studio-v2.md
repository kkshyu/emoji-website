# IG 貼文產生器 v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 後台 IG 產生器升級為 18 版型引擎、修齊 P1-19 發布阻斷，並移除根目錄靜態模板使後台成為唯一 SoT。

**Architecture:** 抽出純函式 `ig-studio-lib.js`（可 node 測試）；`ig-studio.js` 以 layout registry（`1a`–`6c`）渲染預覽／匯出；`admin.html` 登入後動態載入腳本並在切到 IG 分頁時 `resize()`；根目錄 HTML 封存至 `_archive/.../superseded-brand-previews/`。

**Tech Stack:** Vanilla JS、vendored `html-to-image@1.11.11`、Express 靜態站、Node 內建 `node:test`／`assert`。無前端框架。

**Spec:** `docs/superpowers/specs/2026-07-12-ig-studio-design.md`（v2）

---

## File map

| 檔案 | 職責 |
|---|---|
| `public/ig-studio-lib.js` | 純函式：sanitize、scale、酒類判定、警語 HTML、檔名 |
| `public/ig-studio.js` | UI、18 layout、mount／resize／download |
| `public/menu-data.js` | 菜單＋`alcohol` 旗標＋COCOA 修正＋SNACK 註解 |
| `public/admin.html` | 延遲載入腳本、切分頁 resize |
| `public/vendor/html-to-image.js` | 加檔頭 provenance |
| `public/vendor/html-to-image.LICENSE` | MIT 全文 |
| `scripts/test-ig-studio-lib.mjs` | node:test 覆蓋 lib |
| 根目錄→archive | `言文字_IG貼文模板.html` 封存 |

**視覺 SoT（封存前必讀）：** `/Users/kkshyu/Repos/taiwan-talent-hub/言文字_IG貼文模板.html` 的 `#1a`–`#6c` figure。移植時對齊構圖與字級節奏，footer 改讀 `state.handle`／`state.place`（不再寫死 `@emoji0701`）。

## Global constraints

- 不動 `server.js`、資料庫、Stripe。
- CIS：墨／紙／唯一黃 `#FFDE34`、Noto Serif TC／Cormorant／Noto Sans TC。
- 匯出：`await document.fonts.ready` + `htmlToImage.toPng(..., { skipFonts: true, pixelRatio: 1 })`。
- 每個 Task 結束一次 commit（本地 main，不 push，除非使用者要求）。
- 無頭瀏覽器驗證須停用沙盒。

---

### Task 1: 純函式庫 + 測試

**Files:**
- Create: `public/ig-studio-lib.js`
- Create: `scripts/test-ig-studio-lib.mjs`

- [ ] **Step 1: 寫失敗測試**

```js
// scripts/test-ig-studio-lib.mjs
import assert from 'node:assert/strict';
import test from 'node:test';
import {
  sanitizeFilename, computePreviewScale, itemNeedsAlcoholBand,
  alcoholBandHTML, buildDownloadName, PHOTO_MAX_BYTES, isAllowedPhotoType,
} from '../public/ig-studio-lib.js';

test('sanitizeFilename strips path and control chars', () => {
  assert.equal(sanitizeFilename('a/b\\c:美式*.png'), 'abc美式.png');
  assert.ok(sanitizeFilename('x'.repeat(200)).length <= 80);
});

test('computePreviewScale skips tiny widths', () => {
  assert.equal(computePreviewScale(0, 1080), null);
  assert.equal(computePreviewScale(50, 1080), null);
  assert.equal(computePreviewScale(700, 1080), 460 / 1080);
});

test('alcohol flags', () => {
  assert.equal(itemNeedsAlcoholBand({ alcohol: true }), true);
  assert.equal(itemNeedsAlcoholBand({ cat: 'ALCOHOL' }), true);
  assert.equal(itemNeedsAlcoholBand({ note: '含酒精' }), true);
  assert.equal(itemNeedsAlcoholBand({ cat: 'COFFEE', alcohol: false }), false);
});

test('alcoholBandHTML height >= 10% of canvas', () => {
  const html = alcoholBandHTML(1350);
  assert.match(html, /未滿十八歲禁止飲酒/);
  assert.match(html, /禁止酒駕/);
  assert.match(html, /height:\s*135px/);
});

test('photo limits', () => {
  assert.equal(PHOTO_MAX_BYTES, 5 * 1024 * 1024);
  assert.equal(isAllowedPhotoType('image/png'), true);
  assert.equal(isAllowedPhotoType('image/gif'), false);
});

test('buildDownloadName', () => {
  assert.equal(buildDownloadName('美式/咖啡', 'portrait'), '言文字_IG_美式咖啡_直式.png');
  assert.equal(buildDownloadName('x', 'square'), '言文字_IG_x_方形.png');
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd emoji-website && node --test scripts/test-ig-studio-lib.mjs`  
Expected: FAIL（無法 resolve module 或 exports 不存在）

- [ ] **Step 3: 實作 `public/ig-studio-lib.js`**

```js
'use strict';
export const PHOTO_MAX_BYTES = 5 * 1024 * 1024;
export const ALLOWED_PHOTO_TYPES = new Set(['image/jpeg', 'image/png', 'image/webp']);

export function isAllowedPhotoType(type) {
  return ALLOWED_PHOTO_TYPES.has(type);
}

export function sanitizeFilename(name) {
  const base = String(name || 'untitled')
    .replace(/[\/\\?%*:|"<>]/g, '')
    .replace(/[\u0000-\u001f]/g, '')
    .trim() || 'untitled';
  return base.slice(0, 80);
}

export function computePreviewScale(parentClientWidth, canvasW) {
  if (!parentClientWidth || parentClientWidth < 80) return null;
  const avail = Math.min(parentClientWidth - 4, 460);
  if (avail <= 0) return null;
  return avail / canvasW;
}

export function itemNeedsAlcoholBand(item) {
  if (!item) return false;
  if (item.alcohol === true) return true;
  if (item.cat === 'ALCOHOL') return true;
  if (item.note && String(item.note).includes('酒精')) return true;
  return false;
}

/** minHeightPx = ceil(canvasH * 0.10) */
export function alcoholBandHTML(canvasH) {
  const h = Math.ceil(Number(canvasH) * 0.1);
  return `<div class="igp-alcohol" style="height:${h}px;min-height:${h}px;flex:none;display:flex;flex-direction:column;justify-content:center;padding:0 48px;box-sizing:border-box;background:#1B1A17;color:#F4F1EA;font-family:var(--sans);font-size:22px;letter-spacing:.08em;gap:8px;">
    <div>未滿十八歲禁止飲酒</div>
    <div>禁止酒駕</div>
  </div>`;
}

export function buildDownloadName(title, format) {
  const t = sanitizeFilename(title);
  const f = format === 'square' ? '方形' : '直式';
  return `言文字_IG_${t}_${f}.png`;
}
```

若環境無法用 ES module 從 `public/` 匯入：改成 IIFE 掛 `window.IGStudioLib`，測試改讀檔 `vm.runInNewContext`；優先採 ES module（`package.json` 可加 `"type":"module"` **僅當不破壞既有 `server.js` require**——若會破壞，測試改用動態 `fs.readFileSync` + Function 包裝，或把 lib 放 `scripts/ig-studio-lib.mjs` 並由 `ig-studio.js` 內聯複製同一實作。**首選：lib 用 IIFE + `window.IGStudioLib`，測試用 `node --experimental-vm-modules` 或直接：**

```js
// 若採 IIFE：scripts/test 內
import fs from 'node:fs';
import vm from 'node:vm';
const code = fs.readFileSync('public/ig-studio-lib.js','utf8');
const ctx = { window: {}, console };
vm.runInNewContext(code + '\nthis.IGStudioLib = window.IGStudioLib;', ctx);
const lib = ctx.window.IGStudioLib;
```

實作時擇一可跑通的路徑，測試斷言不變。

- [ ] **Step 4: 跑測試確認通過**

Run: `node --test scripts/test-ig-studio-lib.mjs`  
Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add public/ig-studio-lib.js scripts/test-ig-studio-lib.mjs
git commit -m "$(cat <<'EOF'
feat(ig-studio): 抽出可測純函式庫（檔名／縮放／酒類警語）

EOF
)"
```

---

### Task 2: menu-data + vendor provenance

**Files:**
- Modify: `public/menu-data.js`
- Modify: `public/vendor/html-to-image.js`（僅檔頭註解）
- Create: `public/vendor/html-to-image.LICENSE`

- [ ] **Step 1: 更新 `menu-data.js`**

對每個 item 加 `alcohol: true|false`：
- `cat === 'ALCOHOL'` → `true`
- `note` 含「含酒精」→ `true`
- 其餘 `false`

修正：`en:'COCA LATTE'` → `en:'COCOA LATTE'`。

檔案頂部註解加：
```js
/* 價格來自封存 legacy menu，尚未經菜單 owner 核定——後台 UI 須標「僅內部預覽」。
   SNACK：legacy PDF 為全影像，無法可靠還原；暫無 SNACK 品項，待補。 */
```

不虛構 SNACK 價格列。

- [ ] **Step 2: 寫 LICENSE**

將 MIT 全文寫入 `public/vendor/html-to-image.LICENSE`（來源：https://cdn.jsdelivr.net/npm/html-to-image@1.11.11/LICENSE ，Copyright (c) 2017-2023 W.Y.）。

- [ ] **Step 3: 檔頭 provenance**

在 `html-to-image.js` 最上方插入（保留其後 minify 本體）：
```js
/*! html-to-image v1.11.11
 * Source: https://cdn.jsdelivr.net/npm/html-to-image@1.11.11/dist/html-to-image.js
 * License: MIT — see html-to-image.LICENSE
 */
```

- [ ] **Step 4: 驗證**

Run: `node --check public/menu-data.js && test -f public/vendor/html-to-image.LICENSE && head -5 public/vendor/html-to-image.js`  
Expected: 語法 OK；LICENSE 存在；檔頭含 v1.11.11

- [ ] **Step 5: Commit**

```bash
git add public/menu-data.js public/vendor/html-to-image.js public/vendor/html-to-image.LICENSE
git commit -m "$(cat <<'EOF'
chore(ig-studio): 菜單 alcohol 旗標／COCOA 修正與 vendor provenance

EOF
)"
```

---

### Task 3: 重寫 ig-studio 殼層（表單／resize／匯出／P1-19）

**Files:**
- Rewrite: `public/ig-studio.js`
- Depends: `ig-studio-lib.js`（先以 script 標籤載入，或檔內讀 `window.IGStudioLib`）

本 Task **先接上現有 3 型簡化版型**（product／event／quote 對應 `01a`／`02b`／`03a` 占位），確保殼層穩定；Task 4–5 再補齊 18 版。

- [ ] **Step 1: 狀態與預設**

```js
const state = {
  category: '01', variant: 'a', format: 'portrait',
  dark: false, showEn: true, showMember: true, hl: true,
  photo: '', handle: '@emoji0701', place: '重慶南路',
  // product / event / quote / hours / menuRows / brand fields …
  e_when: '', // 空白；placeholder「例：8/9（日）19:00」— 勿寫錯 weekday
};
```

- [ ] **Step 2: `sizeStage` / `resize` / `mount` API**

```js
function sizeStage() {
  const parent = stage && stage.parentElement;
  const { w, h } = DIMS[state.format];
  const scale = IGStudioLib.computePreviewScale(parent ? parent.clientWidth : 0, w);
  if (scale == null) return;
  stage.style.width = w * scale + 'px';
  stage.style.height = h * scale + 'px';
  const node = stage.firstElementChild;
  if (node) {
    node.style.transformOrigin = 'top left';
    node.style.transform = `scale(${scale})`;
  }
}

window.IGStudio = { mount, resize: sizeStage };
```

- [ ] **Step 3: 下載 busy + sanitize + 酒類**

```js
async function download() {
  const btn = root.querySelector('#ig-dl');
  if (btn.disabled) return;
  if (typeof htmlToImage === 'undefined') return toast('匯出元件未載入');
  const needsAlcohol = /* 目前選中品項或 category/variant 標記 */ false;
  // 實作：若 needsAlcohol，postNode 必須含 .igp-alcohol 且高度 >= 10% canvas；否則 toast 並 return
  btn.disabled = true;
  btn.textContent = '匯出中…';
  // … existing offscreen clone + toPng …
  a.download = IGStudioLib.buildDownloadName(title, state.format);
  // finally: btn.disabled=false; btn.textContent='下載 PNG';
}
```

- [ ] **Step 4: 上傳限制**

```js
function readPhoto(file) {
  if (!file) return;
  if (!IGStudioLib.isAllowedPhotoType(file.type)) return toast('僅支援 JPEG／PNG／WebP');
  if (file.size > IGStudioLib.PHOTO_MAX_BYTES) return toast('照片請小於 5MB');
  // FileReader → state.photo
}
```

- [ ] **Step 5: UI 標示未核定**

單品／菜單表單上方：
```html
<p class="hint">菜單價格尚未核定，僅供內部預覽，勿直接對外發布。</p>
```

- [ ] **Step 6: 語法檢查**

Run: `node --check public/ig-studio.js`  
Expected: silent OK

- [ ] **Step 7: Commit**

```bash
git add public/ig-studio.js
git commit -m "$(cat <<'EOF'
feat(ig-studio): 殼層重寫——resize API、匯出 busy、上傳限制

EOF
)"
```

---

### Task 4: Layout registry 01–03（新品／活動／金句）

**Files:**
- Modify: `public/ig-studio.js`
- Read: 根目錄 `言文字_IG貼文模板.html` `#1a`–`#3c`（封存前）

- [ ] **Step 1: Registry 骨架**

```js
const LAYOUTS = {
  '01': { label: '新品／飲品', variants: {
    a: { label: '全幅照片·底部資訊', forceDark: false, render: render01a },
    b: { label: '文字為主·方形照片', forceDark: false, render: render01b },
    c: { label: '深色·框線照片', forceDark: true, render: render01c },
  }},
  '02': { /* 2a 大日期 / 2b 海報框線 / 2c 深色海報 */ },
  '03': { /* 3a 留白大字 / 3b 深色引號 / 3c 照片疊字 */ },
};
function postInner() {
  const L = LAYOUTS[state.category].variants[state.variant];
  return L.render(state);
}
```

切換 variant 若 `forceDark`，設 `state.dark = true` 並禁用暗底 checkbox（或強制勾選）。

- [ ] **Step 2: 實作 `render01a`–`render03c`**

從模板 HTML 移植結構為字串模板：
- 照片：`state.photo` 有值才輸出 `background-image`，否則淺底占位（匯出可不留「拖曳」字）。
- 標題螢光筆：沿用既有 `.hl`。
- Footer：`言文字` + `${place} · ${handle}`。
- 若 `itemNeedsAlcoholBand`（01 選中酒精品）：在 footer **之前**插入 `alcoholBandHTML(DIMS[format].h)`，且 body 其餘區需壓縮以保持總高 = canvas（警語 flex:none，其餘 flex 分配）。

方形比例：各 render 依 `state.format==='square'` 略減字級／照片高度（參考現行 ig-studio 的 sq 分支）。

- [ ] **Step 3: 表單欄位依 category**

- 01：菜單下拉 + eyebrow／中英名／note／desc／unit／price／emo  
- 02：title、dateBig（給 2a）、weekday、when、place、desc、capacity（2a）、signup（2b）  
- 03：q_text、q_sub、q_by  

- [ ] **Step 4: `node --check` + 手動／無頭抽樣**

對 `01a` portrait 與 `03b` square 各匯出一次（若可起站）。  
Expected: PNG 1080 寬；無 JS 錯。

- [ ] **Step 5: Commit**

```bash
git add public/ig-studio.js
git commit -m "$(cat <<'EOF'
feat(ig-studio): 接入 01–03 共九個版型

EOF
)"
```

---

### Task 5: Layout registry 04–06（營業／菜單／品牌）

**Files:**
- Modify: `public/ig-studio.js`
- Read: 模板 `#4a`–`#6c`

- [ ] **Step 1: 擴充 LAYOUTS `04`/`05`/`06`**

| ID | forceDark | 關鍵欄位 |
|---|---|---|
| 4a | false | 多列 hoursRows[{day,time}]、note |
| 4b | false | dayTitle/dayHours/dayDesc、nightTitle/nightHours/nightDesc |
| 4c | true | address、mrt、booking |
| 5a | false | menuTitle、menuRows[{zh,price}]（可多選菜單帶入） |
| 5b | false | coffeeRows、alcoholRows（alcohol 列觸發警語） |
| 5c | true | 單品招牌（同 01 欄位）+ alcohol |
| 6a | false | openingLine、openingDate、openingDesc |
| 6b | false | manifesto（可換行）、en |
| 6c | true | comingTitle、comingSub |

- [ ] **Step 2: 實作 render 函式**

對齊模板構圖。`5b`／任何含 `alcoholRows` 或選中酒精招牌 → 必加警語帶。

- [ ] **Step 3: 表單**

04：可編輯時間列（預設週二–五／六日／週一公休，文案可改）。  
05：從 MENU_DATA 多選或手動列。  
06：開幕／理念／coming soon 文字欄。

- [ ] **Step 4: 驗證 18 keys**

Run:
```bash
node -e "
const fs=require('fs');
const s=fs.readFileSync('public/ig-studio.js','utf8');
for (const c of ['01','02','03','04','05','06'])
  for (const v of ['a','b','c'])
    if (!s.includes('render'+c+v) && !new RegExp(\"'\"+c+\".*[\\\\s\\\\S]*\"+v+\".*render\").test(s))
      console.log('MISSING', c+v);
console.log('scan done');
"
```
Expected: 無 MISSING（或依實作命名調整正則，確保 18 個 render 都存在）

- [ ] **Step 5: Commit**

```bash
git add public/ig-studio.js
git commit -m "$(cat <<'EOF'
feat(ig-studio): 接入 04–06 共九個版型，湊齊 18

EOF
)"
```

---

### Task 6: admin.html 延遲載入 + 切分頁 resize

**Files:**
- Modify: `public/admin.html`

- [ ] **Step 1: 移除 head 外靜態三腳本**

刪除（或註解移除）：
```html
<script src="/vendor/html-to-image.js"></script>
<script src="/menu-data.js"></script>
<script src="/ig-studio.js"></script>
```

改為不在登入前載入。`ig-studio-lib.js` 亦同。

- [ ] **Step 2: 登入成功後載入**

在既有「確認 admin 並 `refresh`／`render`」路徑加入：

```js
async function ensureIgScripts() {
  if (window.IGStudio) return;
  const load = src => new Promise((res, rej) => {
    const s = document.createElement('script');
    s.src = src; s.onload = res; s.onerror = () => rej(new Error(src));
    document.head.appendChild(s);
  });
  await load('/ig-studio-lib.js');
  await load('/vendor/html-to-image.js');
  await load('/menu-data.js');
  await load('/ig-studio.js');
}
```

在通過 admin 檢查後、`render` 前 `await ensureIgScripts()`。

- [ ] **Step 3: bindNav 切到 ig 時 resize**

```js
function bindNav(){
  document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{
    TAB=b.dataset.tab;
    document.querySelectorAll('[data-tab]').forEach(n=>n.classList.toggle('on',n.dataset.tab===TAB));
    document.querySelectorAll('[data-panel]').forEach(p=>p.classList.toggle('on',p.dataset.panel===TAB));
    if (TAB === 'ig' && window.IGStudio && window.IGStudio.resize) {
      requestAnimationFrame(() => IGStudio.resize());
    }
  });
}
```

mount 仍在 `bind()`：`IGStudio.mount(igHost)` 一次。

- [ ] **Step 4: 驗證負縮放修掉**

無頭或手動：登入 → 預設總覽 → 點「IG 貼文」→ 預覽寬度應約 460px 級，`transform: scale` 為正。  
可在 console：`getComputedStyle(document.querySelector('.igp')).transform` 不應含大幅負值。

- [ ] **Step 5: Commit**

```bash
git add public/admin.html
git commit -m "$(cat <<'EOF'
fix(admin): IG 腳本改登入後載入，切分頁重算預覽縮放

EOF
)"
```

---

### Task 7: 封存並移除根目錄模板

**Files:**
- Move: `/Users/kkshyu/Repos/taiwan-talent-hub/言文字_IG貼文模板.html`
  → `/Users/kkshyu/Repos/taiwan-talent-hub/_archive/_整理_2026-07-12/superseded-brand-previews/言文字_IG貼文模板.html`
- Modify: `_archive/_整理_2026-07-12/MOVED_FILES.md`（追加一筆）

根目錄不在 git 內；網站 repo 內確認無引用。

- [ ] **Step 1: 確認後台 18 版已可預覽（Task 5 完成）後再搬**

- [ ] **Step 2: 移動（不刪內容）**

```bash
mv "/Users/kkshyu/Repos/taiwan-talent-hub/言文字_IG貼文模板.html" \
   "/Users/kkshyu/Repos/taiwan-talent-hub/_archive/_整理_2026-07-12/superseded-brand-previews/言文字_IG貼文模板.html"
```

- [ ] **Step 3: 更新 MOVED_FILES.md**

追加列：日期 2026-07-12、理由「SoT 改為後台 ig-studio 18 版引擎；靜態 HTML 僅供追溯」。

- [ ] **Step 4: 確認根目錄無該檔**

```bash
test ! -f "/Users/kkshyu/Repos/taiwan-talent-hub/言文字_IG貼文模板.html" \
  && test -f "/Users/kkshyu/Repos/taiwan-talent-hub/_archive/_整理_2026-07-12/superseded-brand-previews/言文字_IG貼文模板.html" \
  && echo OK
```

- [ ] **Step 5: 網站 repo 無引用**

```bash
cd emoji-website && rg -n '言文字_IG貼文模板' . --glob '!node_modules/**' || true
```
Expected: 無命中（或僅 docs 提及封存路徑）

- [ ] **Step 6: Commit 網站側若有 docs 提及**

若只改 archive（非 git），網站 repo 可更新 spec／plan 註記「根目錄已封存」並 commit：

```bash
git commit --allow-empty -m "$(cat <<'EOF'
chore(ig-studio): 根目錄 IG 模板已封存至 superseded-brand-previews

EOF
)"
```
（若有實際檔案變更則正常 add）

---

### Task 8: 端對端驗收

**Files:** 無強制碼變更；修 bug 則另 commit。

- [ ] **Step 1: 跑單元測試**

`node --test scripts/test-ig-studio-lib.mjs` → PASS

- [ ] **Step 2: `node --check`**

`public/ig-studio.js` `ig-studio-lib.js` `menu-data.js` → OK

- [ ] **Step 3: Spec 驗收清單（對勾）**

依 spec「測試／驗收」1–9 逐項操作或無頭驗證：
1. 未登入 Network 無 ig 腳本  
2. 18×2 預覽無負縮放  
3. 酒精品有 ≥10% 警語  
4. 非酒無強制警語  
5. 上傳拒絕 gif／過大  
6. 下載 busy＋檔名乾淨  
7. vendor LICENSE＋檔頭  
8. 根目錄無模板、archive 有  
9. COCOA＋未核定提示  

- [ ] **Step 4: 最終 commit（若有修補）**

```bash
git commit -m "$(cat <<'EOF'
fix(ig-studio): 驗收修補

EOF
)"
```

---

## Spec coverage self-check

| Spec 要求 | Task |
|---|---|
| 18 layout registry | 4, 5 |
| 酒類 ≥10% 警語／擋匯出 | 1, 3, 4, 5 |
| menu alcohol／COCOA／SNACK 註解 | 2 |
| 負縮放／resize | 1, 3, 6 |
| 上傳限制／檔名／busy | 1, 3 |
| vendor LICENSE | 2 |
| 延遲載入 | 6 |
| 預設 handle／place／日期 | 3 |
| 未核定提示 | 3 |
| 移除根目錄模板 | 7 |
| 驗收 | 8 |

## Placeholder scan

無 TBD／「similar to Task N」空步；版型 Task 以模板 HTML 為 SoT＋registry 表，避免計畫內重複貼 18 份完整 HTML。
